ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [ com.onlygoodthings.backend.ApplicationKt.module ]
  }
}

ogt {
  jdbcUrl = "jdbc:postgresql://127.0.0.1:5432/onlygoodthings"
  jdbcUrl = ${?OGT_JDBC_URL}
  dbUser = "ogt"
  dbUser = ${?OGT_DB_USER}
  dbPassword = "ogt_dev_change_me"
  dbPassword = ${?OGT_DB_PASSWORD}
  redisUrl = "redis://127.0.0.1:6379"
  redisUrl = ${?OGT_REDIS_URL}
  firebaseProjectId = "only-good-things-dev"
  firebaseProjectId = ${?OGT_FIREBASE_PROJECT_ID}
  firebaseCredentialsPath = ""
  firebaseCredentialsPath = ${?OGT_FIREBASE_CREDENTIALS}
  allowDevTokens = true
  allowDevTokens = ${?OGT_ALLOW_DEV_TOKENS}
  parkingProximityMeters = 40
}
